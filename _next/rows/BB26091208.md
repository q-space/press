---
legacy_hash: "391ad876fdea8b4a"
# Frontmatter below is maintained by `slatex sync` from the register --
# state changes there overwrite it. The prose below is yours; edit that.
id: BB26091208
project: "Press"
type: build
subset: null
status: ready
title: "`weekly-status-brief` archetype + field-level AI assist + scheduled invocation"
priority: 3
effort: null
blocked_on: []
claimed_by: null
claimed_at: null
created: "12 Sep 2026"
updated: "1789834071"
---

> new

## Status note (from the legacy row)

⬜ Ready once `BB26091202`+`BB26091203` land

## What

Per canon **D-013** (Sconl's decision, 12 Sep 2026). Three parts, in order. **(1) The archetype** — build `weekly-status-brief` on the node-tree path, modelled on the **implemented flat shape** (`SIGNAL`, `SUBSTANCE`, `TRAJECTORY` as string arrays), *not* iSconl canon §3.2's nested structure which was never implemented (see `BP26091203`, canon D-012). Add as first-class the three fields absent from both: `risks_blockers`, anticipated Q&A, `next_brief_date`. **(2) Field-level AI assist** — a per-field endpoint the creator invokes explicitly, mirroring `spark`'s existing `research-field`/`full-draft` helpers. **The rule is absolute: AI fills one field, never authors the document.** No AI call may exist in `build()` or in any renderer; generation stays byte-identical for a given input and version. **(3) Scheduled invocation** — rebuild the existing Friday auto-draft (currently a `setInterval` in `scope/lib/status-brief.js`) as a scheduled job that **invokes the archetype**. It must not become a second code path that bypasses generation.

## Notes

**Unblocked 12 Sep 2026** — `BP26091202` resolved in favour of the archetype. **Carry these forward from the iSconl investigation so they are not rediscovered:** the existing `gatherActivity()` reads only `tasks.tsv` (filtered by `ORG_ID` + 7 days) and `circle/interactions.tsv` (7 days, **not filtered by subject** — a real bug, see `BF26091202`; port the shape, not the bug). `mondayOf()` is ISO-week correct and UTC-based — keep that. `draftAllBriefs()` deliberately isolates per-subject failure so one bad subject does not block the rest — keep that too. ID allocation as `SB%04d` by max-suffix scan **races** and must not be inherited. **FOLDED IN 15 Sep 2026, PLAN session (`relay-29`) — three things this row needs that were tracked elsewhere.** **(1) The PDF renderer must drive the DevTools Protocol directly, not the Chrome CLI.** From `BP26091301`, verified in session: Chrome's `--print-to-pdf-no-header` switch **no longer exists** (confirmed absent from the binary's strings table, Chrome 151), so a plain `chrome --headless --print-to-pdf` bakes in the browser's default header and footer — page title at the top, URL and page number at the bottom — regardless of that flag. **Use `Page.printToPDF` over CDP (via `puppeteer-core` or an equivalent client) and pass `displayHeaderFooter: false` explicitly.** Confirmed working via a raw CDP call using Node's native `WebSocket`/`fetch` with no package install, as a proof of concept; a real build should use `puppeteer-core` for a maintained abstraction over the same protocol. **(2) The A4 print template pattern** is specified in full on `BB26091204`, where it was folded in the same pass — read it there rather than re-deriving it, and note that both of its traps lose content silently rather than visibly. **(3) Doc ID generation and the document registry belong inside this row.** This was `BA26091105`, one of five weekly-brief rows re-minted `PA26091202` in iSconl and closed there on 15 Sep when that row resolved. Four of the five map onto existing Qpress rows; **this one had no home, and it is not separable from the archetype that generates the document** — an archetype that produces a document without allocating its ID produces an unfilable artefact. Its full original scope is preserved in git at `844b877^:work/dev/Systems/iSconl/_next/backlog/build.md`; **read it there rather than rescoping, since it carries decisions Sconl already made.** **Two authoring-side rules to fold into this archetype's own field guidance and its AI-assist prompt, not just applied once by hand:** cap every bulleted section at three items, which is what actually keeps a one-page brief one page; and no em dashes or other AI-tell phrasing in generated prose.
