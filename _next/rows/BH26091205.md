---
id: BH26091205
project: "Press"
type: manual
subset: null
status: blocked
title: "Reconcile OneDrive — the scrubbed tenant files are still in the backup, and nothing prunes them"
priority: 2
effort: null
blocked_on: ["sconl:decision"]
depends_on: []
claimed_by: null
claimed_at: null
created: null
updated: "2026-09-16"
legacy_file: "hands.md"
legacy_hash: "d065e3c43ac13a8b"
status_note: "🔴 Not verified — flagged, not asserted"
---
> new

## Status note (from the legacy row)

🔴 Not verified — flagged, not asserted

## What

**Your OneDrive, via rclone or the web UI.**

## Exact steps

`backup-relay.sh` does an additive `rclone copy` of the whole relay drive to `Sconl/Crate/.relay` on the `onedrive-sconl` remote, excluding `.git/`. So the *history* should not be there — but the **working-tree files are**, with content intact, and a copy-only backup never removes anything when the source changes. **Two paths to check:** `Sconl/Crate/.relay` and the older un-merged `Sconl/Crate/Relay` (CLAUDE.md §3). Look for `work/dev/Systems/iSconl/scope/lib/generate/archetypes/viva-valentia/` and `.../scope/docs/document-generation-canon.md`. Worth a wider look too: some backup runs went to the wrong remote (`onedrive-acexoft`) before that bug was fixed.

## Notes

**Raised by a peer session and deliberately not asserted as fact** — nobody has actually listed those OneDrive paths. It is the fourth copy of the exposed content: GitHub (scrubbed), the VM clone (`BH26091203`), this machine's clone (scrubbed and gc'd), and OneDrive. The scrub is not complete until this is checked.

