---
id: BB26091205
project: "Press"
type: build
subset: null
status: ready
title: "Thin-client API + re-point iSconl's 13 routes"
priority: 3
effort: null
blocked_on: []
depends_on: []
claimed_by: null
claimed_at: null
created: "12 Sep 2026"
updated: "2026-09-16"
legacy_file: "build.md"
legacy_hash: "63e2f1567c142efe"
status_note: "⬜ Queued — after `BB26091204`; touches iSconl, coordinate"
---
> new

## Status note (from the legacy row)

⬜ Queued — after `BB26091204`; touches iSconl, coordinate

## What

Expose `GET /archetypes?namespace=`, `POST /archetypes/preview`, `POST /archetypes/generate`, `GET /archetypes/:id/schema`. Then re-point iSconl's route mappings in `hub/lib/api-compat.js` from `scope` to Qpress. **`hub` holds no generation business logic at all** — it is already a pure HTTP client with no bespoke handler in `hub/src/server.js` — so this is genuinely 13 mappings plus auth, not a rebuild. iSconl keeps its wizard UI and renders it against `/archetypes/:id/schema` with no local copy of the field definitions. **Auth:** scoped, short-lived, audience-restricted token per canon D-010 — must not carry a creator's full session.

## Notes

**Scoped 12 Sep 2026.** This is the row that actually delivers "thin client." Note it is the first row here that writes into iSconl repos — check for a live session holding those paths before starting.

