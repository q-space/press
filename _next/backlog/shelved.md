# Shelved — paused, not abandoned

<!-- Rows land here from any active category file (plan/build/work/
     light/content, subset sections included) when the whole row, or the whole project, is
     deliberately paused rather than worked or dropped. See
     STANDING-RULES.md "Shelving — pausing without losing" for the full
     mechanics: what moves, how a row is restored, and why this is not
     the same as done.md (nothing here shipped) or deleting a row
     (nothing here is abandoned). -->

<!-- Grouped by originating file, e.g. "### From build.md" — keeps the
     restore path obvious (which file a row goes back to) without
     needing a "From" column repeated on every row. Preserve each row's
     original ID and full content verbatim; only the shelving preface
     line is new. -->
