# H — Hands

<!-- Rows that only Sconl's own hands can resolve — added 12 Sep 2026, per
     Sconl. Not a scoping gap, not a decision waiting on him: work that a
     session physically cannot do no matter how well it is scoped, because
     it needs an account he alone controls, an admin panel a session has no
     credentials for, or a payment only he can make.

     Same rationale that justified opsec.md: plan.md is the routing hub and
     the decision queue, and rows no amount of scoping will ever clear were
     sitting in it indefinitely, making the backlog look blocked when it
     was not. A credential ROTATION still belongs in opsec.md — that split
     is deliberate.

     Sconl tends this file personally. A session's job is to keep the steps
     exact and current, never to attempt the action.

     ID format: B<letter><YYMMDD><NN> per this project's STANDING-RULES,
     so a hands row is BH<YYMMDD><NN>.

     EXCLUDED from "execute the X backlog" runs by definition. -->

| # | Title | What it needs from you | Exact steps | Status | Notes |
|---|-------|------------------------|-------------|--------|-------|
| BH26091501 (new) 🔴 **BLOCKS ALL RUST BUILDS ON THIS MACHINE** | **A Windows Application Control policy is blocking Rust build-script binaries — needs an admin exception, not something a session can work around** | **Admin access to whatever is enforcing this** (Windows Defender Application Control, AppLocker, or "Smart App Control" — a session has no way to tell which from the error alone) on this Windows machine. | **Check Windows Security → App & browser control → Smart App Control** (if present, it's the most likely single culprit — it blocks unsigned/unrecognized executables by default and is common on new Windows 11 installs). If it's on, either turn it off or add an exception. If it's off/absent, check for an AppLocker or WDAC policy via `Get-AppLockerPolicy -Effective` (PowerShell, admin) or `Get-CIPolicy` (`ConfigCI` module) and add a path or publisher exception covering `**\target\debug\build\*\build-script-build` and `**\target\debug\build\*\*.exe` under any repo this machine builds Rust in — not just this one path, since `cargo clean` regenerates these binaries at fresh, unsigned paths every time and the exception needs to survive that. | 🔴 Blocked on you | **Found 15 Sep 2026, BUILD session (`relaybuild-c1`), working `BB26091203`.** `cargo check` on `qspace-press-api` fails deterministically with `error: failed to run custom build command for <crate> ... An Application Control policy has blocked this file. (os error 4551)` — reproduced 3 times, hitting a different crate's build script each time (`async-stripe`, then `serde_json`, consistently on retry), meaning **no Rust crate with a build script can compile on this machine right now**, not just the ones this row happens to name. **This is new since checkpoint 1** (`2ee9d95`, same day, earlier session), which recorded a clean `cargo check` finishing in 54m32s — so either the policy was tightened between then and now, or checkpoint 1 ran under different privileges. **Until this is fixed, no session can get a real `cargo check`/`cargo test` result on this project** — `render_docx.rs`/`render_pdf.rs` (this session's work) and the archetype-registration fix are committed as an unverified checkpoint, explicitly flagged as such, rather than held back indefinitely or falsely marked tested. See `work/_arc/qspace-press/canon-canvas/SESSION-RECORD.md` Open Risks for the same finding. **Once fixed, the very next step for whoever picks this row's project back up is just `cargo check` — no code archaeology needed, the blocker is purely environmental.** **Alternatives checked and eliminated the same night (15 Sep 2026), so this is confirmed a genuine OS-policy fix, not something routable around with a different toolchain:** Docker — not installed on this machine (no `docker` command, no Docker Desktop service; matches this same machine's own documented "Docker-less Windows machine" status per `render-pdf.js`'s doc comment, CLAUDE.md §8-era note). WSL — not installed (`wsl --status` returns "The Windows Subsystem for Linux is not installed"). Neither was installed to work around this — per standing practice, a session doesn't add new system-level tooling to solve an owner's-hands problem. |
| BH26091203 (new) 🔴 **URGENT** | **Run the OCI VM remediation script — removes a live GitHub PAT from 7 checkouts, re-clones `scope` to drop unscrubbed history, and sets 4 env vars** | **A shell on the VM.** A session reached the VM read-only and produced the full survey, but every remote WRITE was refused by the permission classifier (`Remote Shell Writes`). The script is written, idempotent, and prints no credential values. | **The script is at `_recovery/scope-prerewrite-20260912/vm-remediate.sh`** (copied out of the session scratchpad). Run: `scp -i <key> vm-remediate.sh ubuntu@140.238.100.0:/tmp/ && ssh -i <key> ubuntu@140.238.100.0 'bash /tmp/vm-remediate.sh'`. Key comes from Bitwarden `ISCONL_VM_SSH_PRIVATE_KEY` (needs a trailing newline to parse). **What it does, in order:** (1) strips the embedded `x-access-token:<PAT>@` from every remote URL — 7 of 9 checkouts have one: `circle`, `hub`, `media`, `pulse`, `scope`, `spark`, `vault`; `ops` and `qspace-press` are already clean. All repos are public and the deploy only fetches, so token-free HTTPS keeps working. (2) Re-clones `scope` — it **refuses if the checkout is dirty** rather than destroying anything, and moves the old clone aside to `scope.pre-scrub-<stamp>` instead of deleting it. (3) Verifies the scrub on the fresh clone. (4) Appends 4 `SCOPE_*` vars to `deploy/.env`, backing it up first and skipping any key already present. (5) Discards the now-committed `qspace-press` Dockerfile edit. (6) Final credential sweep. | 🔴 Blocked on you — classifier refused remote shell writes | **Why the re-clone and not a reset:** `git fetch` + `reset --hard` recovers the *checkout* but does **not** remove objects. The VM's `scope` clone still holds **3 commits touching the deleted tenant archetype and 18 reflog entries** — readable by anyone with shell access. Only a genuine re-clone (or an aggressive reflog-expire + gc) removes them. A reset would look like it worked. **Why the env vars matter:** `staging` now carries code where four tenant paths read from the environment with no default. That is deliberate and safe — features no-op rather than writing to a wrong folder — but OneDrive document push, the binder, and deliverable collection stay inert until these are set. **Delete `scope.pre-scrub-<stamp>` only after verifying the fresh clone works.** |
| BH26091204 (new) 🔴 **URGENT** | **Rotate the GitHub PAT — it has been live and leaked twice, and a rotation row has been open since 5 Sep unactioned** | **Your GitHub account.** A classic PAT cannot be minted from the CLI; it needs the web UI. | GitHub → Settings → Developer settings → Personal access tokens → revoke the current `isconl` token and issue a replacement. **Then decide whether anything still needs it at all:** after `BH26091203`, no VM remote embeds a credential, and the deploy only *fetches* public repos, so a token may not be required on that host any more. If something does need one, prefer an SSH deploy key or a credential helper over re-embedding a token in a URL — see canon D-016. Also update Bitwarden `ISCONL_GITHUB_TOKEN`. | 🔴 Blocked on you | **This is the most urgent item on the board.** The token is live, carries push rights to `isconl/*`, and sat in plaintext on a host whose repos are all public. **It leaked twice through `git remote -v` — 5 Sep and 12 Sep, seven days apart — and `KI26090604` was opened for the first one and never actioned.** Two transcripts now contain it. Rotation is the only thing that makes those transcripts harmless; everything else is mitigation. Sequence this **after** `BH26091203` so the VM is not re-pointed twice. |
| BH26091205 (new) | **Reconcile OneDrive — the scrubbed tenant files are still in the backup, and nothing prunes them** | **Your OneDrive, via rclone or the web UI.** | `backup-relay.sh` does an additive `rclone copy` of the whole relay drive to `Sconl/Crate/.relay` on the `onedrive-sconl` remote, excluding `.git/`. So the *history* should not be there — but the **working-tree files are**, with content intact, and a copy-only backup never removes anything when the source changes. **Two paths to check:** `Sconl/Crate/.relay` and the older un-merged `Sconl/Crate/Relay` (CLAUDE.md §3). Look for `work/dev/Systems/iSconl/scope/lib/generate/archetypes/viva-valentia/` and `.../scope/docs/document-generation-canon.md`. Worth a wider look too: some backup runs went to the wrong remote (`onedrive-acexoft`) before that bug was fixed. | 🔴 Not verified — flagged, not asserted | **Raised by a peer session and deliberately not asserted as fact** — nobody has actually listed those OneDrive paths. It is the fourth copy of the exposed content: GitHub (scrubbed), the VM clone (`BH26091203`), this machine's clone (scrubbed and gc'd), and OneDrive. The scrub is not complete until this is checked. |
| BH26091201 (was `PP26090901`) | **Pre-Cycle 0 external accounts — 4 signups remaining, each needing identity or payment details a session does not have** | **Your own signups.** A build session can hand-carry credentials *into* env config once they exist, but cannot create the accounts. | **(1) Resend — ✅ DONE.** Account created via GitHub login; `mail.acexoft.com` verified (not `mail.qspacepress.com` — per your 9 Sep call, this generalises across every acexoft-brand service and avoids waiting on `BH26091202`'s domain purchase). All 4 DNS records (DKIM, SPF CNAME, SPF TXT, DMARC) added in Cloudflare's `acexoft.com` zone. Resend's free tier allows 3 domains, leaving room to add `mail.qspacepress.com` later for production branding. **Confirm the status has moved off "Pending"** — it was still propagating when last checked. **(2) Sentry** — error tracking. Sign up, create a project, keep the DSN. ~2 minutes, no business details. Blocks Week 4's wiring step. **(3) BetterStack** — uptime monitor. Same shape, ~2 minutes. **(4) M-Pesa Daraja** — sandbox credentials. Likely needs a Safaricom developer account plus business registration details; **historically the slowest approval of the set, so start it early even though it blocks nothing until Cycle 3.** **(5) Stripe** — test-mode account, for Cycle 3's international fallback path. Needs real business details. | 🟡 (1) done pending propagation; (2)–(3) next | **(2) and (3) are the low-friction batch** — no paperwork, both done in one sitting. **(4) and (5) are deliberately deferred** to closer to Cycle 3; neither is worth front-loading before Cycle 1 ships. **Credential storage:** goes into Bitwarden, pulled by build sessions key-only per `CLAUDE.md` §17, never pasted into a session. **Open, and worth settling before the first secret is written:** this session's standing recommendation is a **new dedicated `qspace-press` Secrets Manager project** rather than folding into `isconl`'s — it is a genuinely separate venture with its own credential surface. Not yet confirmed with Sconl. **Narrowed twice on 9 Sep 2026** — Supabase and Upstash removed once OCI self-hosting replaced them (`BB26090903`/`BB26090904`), then Cloudflare R2 removed once it turned out to require a billing subscription on file even for free-tier use, replaced by self-hosted MinIO. 8 signups became 5, now 4 outstanding. Moved from `plan.md` to `hands.md` 12 Sep 2026, per Sconl. |
| BH26091202 (was `PP26090902`) | **Register `qspacepress.com` — $10.46/year at Cloudflare Registrar, deferred until your Cloudflare account is funded** | **Your Cloudflare account with billing loaded, and the purchase itself.** Explicitly deferred by you 9 Sep 2026 ("can we defer it? i havent loaded the account") — a sequencing choice, not a scoping gap. | Cloudflare dashboard → **Domains → Registrations → Buy domain** → search `qspacepress.com` → complete the purchase. **Use the same Cloudflare account that already holds `acexoft.com`** — registering there means the domain is immediately usable as a DNS zone with no separate "add to Cloudflare" step; registration and zone setup happen together. Cloudflare Registrar sells at cost with no markup. | 🔴 Deferred, waiting on you | **Confirmed available and priced at $10.46/year, "Top pick," live check 9 Sep 2026** — re-verify before purchase, since availability and price can both move. **Found mid-`BH26091201`'s Resend domain-verification step:** `mail.qspacepress.com` could not be DNS-verified because the apex domain does not exist on any registrar. **No longer blocks `BH26091201`** — you redirected that row to verify `mail.acexoft.com` instead, so Resend proceeded without waiting. **Still blocks:** the real production hosting target (`main` branch, per the canon doc's own `https://qspacepress.com` target) and adding `mail.qspacepress.com` as Resend's brand-facing production sender. **Does not block `BB26090904`'s staging work**, which lives entirely under the already-owned `qpress.acexoft.com`. Moved from `plan.md` to `hands.md` 12 Sep 2026, per Sconl. |
