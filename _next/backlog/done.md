# Done — recently shipped work

## DONE THIS SESSION (2026-09-16, LIGHT session -- housekeeping, no code change)

- **BO26090901** (Organize subset): the `app/{web,api,mobile}` monorepo restructure shipped as `3e743cd` on 9-10 Sep 2026 and its status cell was corrected to done on 12 Sep, but the row itself was never moved out of the category file. A Light run therefore kept surfacing it as the top of this project's queue. Moved to `done.md`; the original row is retained as a provenance comment in `light.md`. Nothing about the restructure changed. The follow-up the row asked for -- correcting all 19 stale `apps/` references in the canon doc -- had already landed in canon v2.0.0 on 12 Sep.

## DONE THIS SESSION (2026-09-15, LIGHT session)

- **BO26091501** (Refine subset): the actionable part shipped — the desktop-targets requirement is now written into canon, not left waiting in `plan.md`/`light.md` for someone to open. New canon file `work/_arc/qspace-press/canon-canvas/20260915_project_development_canonical_qspace_press_v2_1_0.md` (v2.0.0 kept, never overwritten): new Decision D-017 states desktop is a standing target for `app/mobile`, explicitly flagged against the real tension this creates with D-011's phone-only framing rather than silently resolved — the UX/naming questions stay open on purpose, for whoever next touches the scaffold. Also flagged directly in the "Application Surfaces" table/section. `SESSION-RECORD.md` (Claude-facing, read-first record) got the same decision logged, v1.2.0 → v1.3.0. Same requirement written into **qspace-pages**' `canon-canvas/SESSION-RECORD.md` too (v1.0.0 → v1.1.0), per this row's own instruction not to leave it QSpace-Press-only — flagged there against that project's own architecture drift (the latest canon-canvas file describes an older Flutter-Web app surface, not the native mobile+desktop direction `BB26091401` confirmed 14 Sep). **Not done, per the row's own explicit deferral:** confirming the three desktop targets actually build (needs a compiling scaffold — `BB26091203`'s Rust build is itself blocked on `BH26091501`, a Windows Application Control policy issue only Sconl can fix), and deciding the `app/mobile` rename (`app/companion` vs `app/desktop-mobile` vs keeping the name) — both explicitly "when the scaffold is next touched," not now.

## DONE THIS SESSION (2026-09-15, FIX session — housekeeping, no code change)

- **BF26091201**: was marked ✅ DONE (12 Sep 2026, Cloudflare Tunnel ingress fix for `qpress.acexoft.com`) but never moved out of `fix.md`, and the row was duplicated (identical text twice). Moved here, deduplicated — no new work, the fix itself already shipped and was verified 12 Sep.
- **BF26091202**: was a cross-reference note pointing at iSconl's own row for the same defect, not a Qpress-owned task. The real fix landed as `FA26091201` in `work/dev/Systems/iSconl/_next/backlog/done.md` (`scope` `dev` `85f3486`, 15 Sep 2026 FIX session) — `gatherActivity()` now resolves the subject's people via the same GROUP<->SOURCE_REF match `active-subjects.js` uses, instead of pulling every interaction in the window regardless of subject. Removed from this project's `fix.md`; nothing further needed here.

## DONE THIS SESSION (2026-09-15, BUILD session — housekeeping, no code change)

- **BB26091202** (`render-html.js` fourth renderer): row closed 12 Sep 2026 (premise was wrong — `render-html.js` already existed, committed to `isconl/scope` as `d12a832`, already wired into `generate-client.js`) but never moved out of `build.md`. Moved here now; no new work done. See build.md git history for the full investigation notes (three independent readers concluded absence from stale checkouts).
- **BB26090904** and **BB26091201**: both reached terminal DONE status (see the 2026-09-12 block below) but also sat un-moved in `build.md`. Removed from the live table; their full detail is already captured below.

## DONE THIS SESSION (2026-09-12, PLAN session — repo renamed, 3-branch model, canon v2.0.0)

- **BO26091201**: Local checkout renamed `qspace-press-repo` → **`Press`**, per Sconl. Not a plain rename — a stale Windows file handle on `node_modules/.pnpm/@isaacs+cliui@8.0.2` made the directory unrenameable (Windows refuses a directory rename when anything in the subtree holds an open handle; no owning process was visible via `Get-CimInstance`). Narrowed the lock by bisecting renames down the tree, then moved everything except `node_modules` with `robocopy /MOVE /XD node_modules`. **One real incident inside this:** the first pass excluded only the *root* `node_modules` by absolute path, so it began copying the nested ones; killing it mid-flight lost `packages/shared`'s 8 tracked files from both source and destination. All were at `HEAD` and were recovered with `git restore packages/shared` — verified, working tree returned to exactly its pre-move state (`M pnpm-lock.yaml` + the untracked `deploy-staging.yml`, unchanged). **Leftover:** a `qspace-press-repo/` stub holding only `node_modules`, still undeletable; nothing tracked in it, `pnpm install` regenerates it, safe to remove after a reboot. Registry `LOCAL_PATH` updated.

- **BB26091201**: Captured the VM-only `app/api/Dockerfile` edit — builder base `rust:1.87-slim` → `rust:1.97-slim`. This had been made directly on the OCI VM during the 12 Sep deploy and existed in **exactly one place** (`~/isconl-docker/qspace-press`, uncommitted, no backup, no copy in any local checkout). Now committed as `08395bd` and pushed, so it reaches the VM the normal way. Commit message records that the bump was almost certainly forced — a real MSRV floor, not a preference — and that it stays pinned, so the file's own reproducibility rationale is unaffected.

- **Three-branch model created**, mirroring the iSconl fleet exactly: `dev` (all development, never deploys) · `staging` (push = deploy → `qpress.acexoft.com`) · `main` (release → `qspacepress.com`). All three pushed to `q-space/press` at `08395bd`; local checkout now sits on `dev`. This closes the gap flagged 11 Sep, when `git ls-remote` confirmed only `main` existed and the repo diverged from the fleet's own branch policy.

- **Canon bumped to v2.0.0** — `work/_arc/qspace-press/canon-canvas/20260912_project_development_canonical_qspace_press_v2_0_0.md` (renamed from the 20260325 v1.0.0 filename). Nine substantive changes: status corrected (v1.0.0 still read *"Pre-Cycle 0, `main`, no commits yet, Blockers: None"* against 8 commits, a building scaffold and a live staging deploy); **Feature 5 added** (Document Generation & Archetypes — the second output mode); **business model rebuilt** (D-008); **application surfaces defined** — Reader / Creator Studio / Creator Companion / thin clients, closing the previously undefined `app/mobile`; **three auth paths** specified (D-010); infrastructure rewritten to self-hosted OCI; branch model documented; `apps/` → `app/` throughout (19 occurrences); and **two internal contradictions resolved** — the Cycle-8 MRR target appeared as both KES 2,000,000 and KES 1,000,000 in two tables, and *"Free Publishing Is Unconditional"* contradicted the pricing table two sections later, which already metered email at 1,000/month. New decisions **D-008**–**D-012**; **D-007 explicitly superseded**. Sections knowingly left stale are listed in the doc itself rather than left to be discovered.

- **Scoping pass**: 6 new `plan.md` rows (`BP26091201`–`BP26091206`), 7 new `build.md` rows (`BB26091201`–`BB26091207`), 2 new `fix.md` rows (`BF26091201`–`BF26091202`). `BB26090904` closed out — the staging deploy landed via a resumed crashed session and was verified end-to-end.

**Two rows blocked on Sconl and flagged 🔴:** `BP26091202` (weekly brief — deterministic archetype vs AI-drafted pipeline) and `BP26091206` (timeline re-baseline). Neither is a scoping gap; both are genuine forks only he can choose.


<!-- Rolling window, kept short enough to skim. Older blocks move to
     done-archive/YYYY-MM.md — see STANDING-RULES.md "Done-list
     retention." -->

## DONE THIS SESSION (2026-09-09/10, BUILD session — Pre-Cycle 0 Week 1 executed top to bottom)

- **BO26090901**: Restructured the repo into `app/{web,api,mobile}` — cloned `q-space/press` locally for the first time (it had no local checkout at all), moved the existing Flutter scaffold into `app/mobile/` via `git mv` (full history preserved, confirmed as renames not delete+recreate), added `pnpm-workspace.yaml`, a monorepo root README, and a fresh root `.gitignore`. Pushed `main` `3e743cd`.

- **BB26090901**: Scaffolded `app/web` (Next.js 14, hand-built rather than via `create-next-app` — its interactive installer was unreliable in this environment, and the canon doc specifies an exact non-default route-group tree anyway) — full `(marketing)/(auth)/(creator)/(publication)` route-group tree, `api/webhooks/{mpesa,stripe}` + `api/auth/[...nextauth]` routes, every route a real working "not yet built" placeholder, not a 404. `packages/shared`: Publication/Post/Subscriber/Subscription/Payment Zod schemas, full field lists ported from the canon doc's SQL Data Model. `app/api` (Rust Axum): full `src/` tree per the canon doc (auth/publications/posts/audience/payments/email/distribution/analytics/storage), 3 migration files (8 tables, indexes) ported verbatim from the canon doc. **One real deviation, flagged not silently made:** the canon doc's pinned `stripe-rust` crate is dead on crates.io (every version transitively depends on a fully-yanked `crypto-mac`, confirmed via `cargo check`) — swapped to `async-stripe`, zero blast radius since nothing calls Stripe yet. Verified: `pnpm install`/`typecheck`/`lint` clean, `next build` succeeds (12/12 routes), `cargo check`/`cargo test`/`cargo build` all clean, `GET /health` confirmed live via a real running process → `200 ok`. Pushed `main` `c091f32` + `312d8f3`.

- **BB26090903**: Docker Compose local dev stack — `web` (3000), `api` (3001), `postgres` (5433), `redis` (6380), `minio`+`minio-init` (9000-9001, auto-creates the `qspacepress-media` bucket). MinIO replaces Cloudflare R2 per a concurrent same-day PLAN decision (no billing needed before a real user exists) — `app/api`'s S3 client code is unaffected, same `aws-sdk-s3` talks to either via `S3_ENDPOINT` alone. **Three real bugs found and fixed by actually booting the full stack, not just validating config:** (1) `rust:1.79-slim` too old for `cargo-watch`'s own transitive deps (needed 1.85, then 1.87 minutes later) — moved to untagged `rust:slim` rather than keep re-chasing a pin. (2) Container-internal service URLs were wrong — `.env`'s `DATABASE_URL`/`REDIS_URL`/`S3_ENDPOINT`/`QSPACE_API_URL` use host-mapped ports, unreachable from inside another container; fixed with `environment:` overrides using Docker's internal service DNS. (3) `web`'s `next dev -- -H 0.0.0.0` didn't forward through `pnpm` correctly (Next.js treated `-H` as a positional directory) — fixed via `pnpm exec next dev -H 0.0.0.0` directly; also caught `web` silently inheriting `api`'s `PORT=3001` from the shared `.env` file. Verified live end-to-end: postgres accepting connections, redis `PONG`, minio bucket created, all 3 migrations applied against real Postgres (8 tables confirmed via `\dt`), `api`'s health check and `web`'s `/` both `200` through their actual containers. Torn down clean after (`docker compose down -v`). Pushed `main` `930c325`.

- **BB26090902**: GitHub Actions CI (`web` job: typecheck/lint/test across the whole workspace; `api` job: `cargo check` → `cargo clippy -D warnings` → `cargo test`) + `CODESPACE_RULES.md` (real content existed in the canon doc's Appendix G, not a placeholder — checked before assuming). **Verified the clippy gate would actually pass before committing it, not assumed:** ran it in a real `rust:slim` container with `-D warnings` (no rustup on this machine to test locally otherwise) — failed on the same two intentional "not yet wired in" dead-code items `cargo check` already flagged (`Config`'s unused fields, `db::connect`); added explicit `#[allow(dead_code)]` with reasoning rather than weaken `-D warnings` crate-wide. Re-ran clean. `CODESPACE_RULES.md` explicitly flags that the scaffold committed before it (`BO26090901`/`BB26090901`/`BB26090903`) does not yet follow its own rules — a retrofit is a separate task, not silently assumed covered. Pushed `main` `cb0a095`.

**Left correctly open, not cleared:** `BB26090904` (staging deploy to `qpress.acexoft.com` via the existing Cloudflare Tunnel) — still blocked on SSH access to the OCI VM, which this session doesn't have.
